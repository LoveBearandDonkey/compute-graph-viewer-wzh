# 需求解决方案总览表

| # | 分类 | 用户问题（摘要） | 解决方案 | 结合 AI 能力的解决方案 | 设计点（宣传 Slogan） | 对应定位链案例 |
|---|---|---|---|---|---|---|
| 1 | 🟦 算子性能调优 | Vector 计算类无 API 耗时资料；多核同地址竞争让并行变串；profiling 缺每核耗时；希望工具能读 profiling+代码+用例，或设计阶段模拟多种流水方案<br><br>原声："我们用表格按矩阵大小算搬运量，再除以带宽算利用率；但Vector计算类没有资料，不知道每个API耗时多少。多核同地址竞争会让并行变串，只能靠软件规避。profiling有时没有每核耗时数据，界面问题也不知道反馈给谁。希望工具能读profiling、代码和用例，或者在设计阶段模拟多种流水方案。" | 建立 Vector/Cube API 耗时基准库（按 shape/dtype 打表）；profiling 补齐 per-core 耗时字段；提供同地址访问冲突检测；开发流水编排模拟器，设计阶段对比多种 tiling/流水方案的理论耗时与利用率 | AI 联合读取 profiling+源码+用例，自动识别 Vector 段并估算各 API 耗时；对同地址竞争定位到"哪几行在抢同一 bank/地址"并给规避改法；按算子语义自动生成候选流水方案并排序推荐 | 「所见即所耗——每条 Vector 指令都有价签」<br>「设计即预演：流水方案未写先算」<br>「多核不再暗战，冲突一眼归位」 | 性能案例一（扩展：算子开发视角——Vector API 耗时基准 + 多核竞争检测 + bank 冲突回溯） |
| 2 | 🟦 算子性能调优 | 调优核心在"怎么切"（如 InstanceNorm 遍历方向影响搬运次数）；bank 冲突时希望指出前后两次冲突访问分别对应哪两处代码<br><br>原声："调优主要在怎么切。比如InstanceNorm遍历方向不同，搬运次数就不同，竖直方向遍历可以复用数据、少搬几次。bank冲突发生时，我还希望工具能指出前后两次冲突访问分别对应哪两处代码，才能有针对性地改。" | 切分策略顾问：基于遍历方向分析数据复用与搬运次数，推荐最优 tiling 与遍历方向；bank 冲突分析器：把每次冲突访问回溯到源码两处访存点并成对高亮 | AI 依算子计算模式推演"竖直/水平遍历"等切分对搬运量的影响并给量化收益；对 bank 冲突生成"前一次在 L#、后一次在 L#，改为 XX 可消除"的自然语言修复建议 | 「切一刀，省几趟——搬运量看得见」<br>「冲突现形，代码对号入座」 | 同上 |
| 3 | 🟦 算子性能调优 | 找到已有算子也不知其性能水平；模型侧只有端到端目标，给不出单算子应达指标；缺实测与基线<br><br>原声："即使找到已有算子，我也不知道它现在的性能水平；模型侧只有整体端到端目标，也给不出这个算子应该达到多少。没有实测和基线，单算子性能指标很难制定。" | 算子性能基线库（roofline+实测），给出该 shape 理论上限与同类算子实测分位；端到端目标按算子占比自动拆解出单算子预算 | AI 依模型结构与端到端目标反推每个算子的目标耗时，结合 roofline 判断"还有多少可优化空间/是否已接近上限" | 「每个算子都有一条及格线」<br>「从端到端目标，一键落到每颗算子的预算」 | 性能案例一（Roofline 分析 lm_head 带宽瓶颈）、性能案例二（MFU 拆解为达成率×占用率） |
| 4 | 🟦 算子性能调优 | 自动识别并理论估算不同模型/算子耗时开发量大；算子文档不完备，需工具团队+算子团队联合开发<br><br>原声："针对不同模型、不同算子自动识别并理论估算耗时，开发量很大；算子文档又不完备，还需要MindStudio工具团队和算子团队联合开发" | 构建算子知识库+理论耗时模型模板，工具团队与算子团队共建、增量维护；文档缺口用可执行的性能标注补齐 | AI 从算子实现与 profiling 自动抽取性能特征反哺文档；以少量标注冷启动、持续学习各模型各算子耗时估算模型，降低联合开发成本 | 「让算子自己写下它的性能说明书」<br>「模型自学，文档自长」 | —（平台建设需求，暂不设案例） |
| 5 | 🟦 通信类 | 通信场景算子性能异常（卡故障/负载不均/通信算子耗时变长）；排查链路长；希望报错像 PyTorch 直接反映问题与处理方法<br><br>原声："通信场景会遇到各种算子性能异常。有时是某张卡故障导致同步异常，有时是负载不均，通常表现为某个通信算子耗时变长。我们会先排查算子下发和卡间互联，解决不了再找HCCL或算子同事。希望报错能像PyTorch一样直接反映问题和处理方法，尽量减少依赖更专业的同事。" | 通信异常一键体检：卡故障/负载不均/下发/卡间互联分层自检，输出结构化根因与处理建议；对齐 PyTorch 风格可读报错 | AI 把 HCCL 底层信号翻译成"问题是什么+大概率原因+下一步怎么做"，并判断该自查还是转 HCCL/算子专家，缩短求助链路 | 「通信报错，说人话」<br>「从'找谁'到'照做'——排查链路一步到位」 | Infra 案例（NVLINK 掉线排查）、性能案例三（all-to-all straggler 定位）、精度案例一 §3（通信调度层：per-rank timeline 对比 all-to-all 超时 + send/recv 不匹配 → 结构化诊断："问题是什么+大概率原因+下一步怎么做"，对齐 PyTorch 风格可读报错） |
| 6 | 🟦 通信类 | Timeline 里 HCCL Slice Detail 信息不全；communication JSON 有更细 transmit/wait/RDMA/分链路，希望解析展示；只分通信/计算/free 三类，需更细 PP/DP/TP、空泡、负载不均等待，专家手拆两三小时<br><br>原声："Timeline里HCCL通信算子Slice Detail信息不全，profile里的communication JSON有更详细的transmit time、wait time、RDMA、HCCL分链路信息，希望解析进来在界面能看。工具现在只能把通信、计算、free分三类，我们要更细的PP、DP、TP通信、空泡、负载不均等待，只能专家自己拆，每次可能两三个小时。" | 解析 communication JSON，界面呈现 transmit/wait/RDMA/分链路明细；把三分类细化为 PP/DP/TP 通信、空泡、负载不均等待等语义泳道，自动拆解替代人工 | AI 依并行策略与时序自动归类每段通信属于哪种并行维度，识别空泡与负载倾斜来源，一键产出"两三小时手拆"的结果 | 「通信不再只有三种颜色」<br>「专家的三小时，交给一次点击」 | 性能案例三（communication JSON transit/wait 瀑布图拆解 all-to-all） |
| 7 | 🟦 内存调优 | 内存分析要用内存快照，存 pkl 后用专门网页解析生命周期/堆栈；MindStudio Insight 看不出内存碎片<br><br>原声："内存分析还要用内存快照，保存成pkl后用专门网页解析，看每一块内存申请的生命周期、堆栈是哪块代码触发的。MindStudio Insight现在看不出来内存碎片，所以才用那个工具。" | 把内存快照解析内建进工具，直接可视化每块内存的生命周期、申请堆栈与碎片分布，免去导出 pkl 再换网页 | AI 定位碎片成因（哪段代码的申请/释放模式导致碎片），给出复用/池化/重排建议 | 「碎片无处遁形，堆栈一键回溯」<br>「一个页面，看穿每块显存的前世今生」 | 🆕 性能案例四（显存分支：activation checkpoint 未开启→峰值超标 + 分配器碎片→OOM，利用定位链分支C框架） |
| 8 | 🟨 内存调优（竞品/对标） | MindStudio Insight 以折线图展示显存变化，可从峰值算子跳转时间线，再回到 Python 调用栈定位<br><br>原声："MindStudio Insight以折线图展示显存变化，还可以从峰值附近的算子跳转到时间线，再回到Python调用栈定位问题。" | 沿用并强化"显存曲线→峰值算子→时间线→Python 调用栈"的下钻闭环，作为标准能力 | AI 在峰值处自动标注"谁此刻吃显存最多、能否提前释放/重计算"，给出降峰方案 | 「顺着峰值一路点回那一行代码」<br>「显存曲线会自己讲故事」 | —（竞品对标需求，暂不设案例） |
| 9 | 🟦 整网性能调优 | 优化多少难评估、目标靠经验 PK；英伟达 Nsight 能看算子实搬多少数据，我们缺，端到端难拆单算子<br><br>原声："做模型性能分析时，有些地方需要优化算子或做融合算子，但能优化多少很难评估，目标只能靠经验PK。英伟达Nsight能看到算子实际搬了多少数据，我们缺少这类数据，端到端指标很难往单算子拆。" | 补齐算子级实际搬运量、算力利用率等硬指标（对标 Nsight），让端到端目标可按真实数据往单算子拆 | AI 基于实测搬运量与 roofline 估算"这个算子/这次融合还能优化多少"，把"经验 PK"变成量化收益预测 | 「优化多少，先算给你看」<br>「让每字节搬运都留下账本」 | 性能案例一（cube_util 分析 + 搬运量估算）、性能案例二（MFU 公式拆解，量化各因子贡献） |
| 10 | 🟦 整网性能调优 | 想把整网拆到每层/每个局部（如 MoE 一堆算子）看平均耗时（涉及负载不均）；现在不直观，原始数据要再提炼<br><br>原声："我们想把整网拆分出来每一层、每一个局部，比如MoE这一块一堆算子的耗时，而且要看平均，因为还涉及负载不均、每层耗时不一样。现在要获得这个信息不直观，原始数据是有的，只是要再提炼。" | 整网分层下钻，按层/结构块（如 MoE）聚合耗时并展示均值/分布，直接从原始数据提炼，无需人工再加工 | AI 自动按模型结构切分层次、识别负载不均层，给出"哪一层最该优化"的排序 | 「从整网到 MoE 一块，逐层看得清、看得均」<br>「原始数据里的金子，自动淘出来」 | 精度案例三（逐层误差累积曲线，layer 35 拐点定位）、性能案例一（PP stage 定位层拆解） |
| 11 | 🟨 整网性能调优（竞品/对标） | 架构与性能联动：靠全局时间戳/算子ID/图节点ID/rank/张量ID 建映射，导入 graph IR 采集耗时/算力/显存/通信挂到 Transformer/Attention/FFN，三层下钻查异常 rank、气泡、倾斜、通信等待等<br><br>原声："架构与性能联动依靠全局时间戳、算子ID、计算图节点ID、rank及张量ID建立映射。工具先导入PyTorch graph IR，再在训练过程中采集算子耗时、算力、显存和通信数据，并挂载到Transformer、Attention、FFN等模型节点。界面按集群拓扑、DP/TP/PP/MoE并行时序、模型与算子联动三层下钻，可查看异常rank、流水线气泡、负载倾斜、通信等待、权重读取、梯度计算及跨rank规约。" | 建立五要素统一映射，导入框架 graph IR，采集并挂载至模型节点；集群拓扑→并行时序→模型算子联动三层下钻 | AI 自动维护动态图下的 ID 映射，自动标注异常 rank/流水气泡/负载倾斜/通信等待 | 「一张图，从集群拓扑钻到一颗张量」<br>「架构与性能，同频共振」 | 精度案例三（误差传递因果链，从 layer 35 沿计算图逐级追踪）、性能案例一（stage/算子定位层联动） |
| 12 | 🟨 整网性能调优（竞品/对标） | 配套 Expert System：识别 GPU 空转、频繁 stream 同步、低效拷贝并标红；按算子/通信/显存/并行标注瓶颈，把定位从大半天缩到几十分钟；但新手仍需更直白的原因解释与代码关联<br><br>原声："联动视图同时配套Expert System诊断能力。12.4以上版本已能识别GPU空转、频繁CUDA Streams同步和低效显存拷贝，并在界面中标红提示；更深层的自动化诊断仍在头部千卡客户灰度，可按算子、通信、显存和并行策略标注瓶颈，将部分性能定位从大半天缩短到几十分钟至一小时。新手仍需要更直白的原因解释、操作建议和代码关联，避免看到专业指标后仍要转向文档理解。" | 内置专家规则库，识别核空转/频繁同步/低效拷贝并标红，按算子/通信/显存/并行标注瓶颈 | AI 把"看到红点"升级为"给新手的原因解释+操作建议+代码关联"，把定位从大半天缩到几十分钟 | 「不止标红，更告诉你为什么红、怎么改」<br>「专家诊断，人人可用」 | 精度案例一~三、性能案例一~三（均为"专家诊断路径"的完整示范，含原因解释+操作建议+代码修改+验证） |
| 13 | 🟨 整网性能调优（竞品/对标） | 全埋点 trace 带来 15%~18% 训练开销，轻量采样又缺细粒度信息；动态控制流临时 ID、跨节点只关联到 rank、异构埋点不统一、超大 trace 加载慢<br><br>原声："当前主要瓶颈包括动态控制流产生临时算子ID、跨节点通信只能关联到rank、异构硬件埋点和指标标准不统一，以及超大trace加载缓慢。完整CUDA trace与NVTX全埋点约带来15%至18%的训练开销，轻量采样又会缺失权重、梯度等细粒度信息。现阶段采用多面板联动和按并行维度折叠展示；后续重点是按需分片采集、过滤无效区间、完善自动瓶颈标注，并提高异构环境下的关联能力。" | 按需分片采集、过滤无效区间、完善自动瓶颈标注，兼顾细粒度与开销；提升异构环境关联能力 | AI 智能决定"何处该细采、何处可粗采"，在开销与信息量间自适应，并补齐异构关联 | 「该细则细，该省则省——采集也懂取舍」<br>「低开销，不丢真相」 | —（竞品对标需求，暂不设案例） |
| 14 | 🟦 性能对比 | 多卡比较信息多，只能手动把几张卡时间线放一起肉眼比对；希望自动判断多卡 profiling 是否对齐、分析算子异常及原因<br><br>原声："多卡比较时信息很多，单卡还能看，多卡只能手动把几张卡的时间线放到一起，逐一盯着肉眼比对。希望工具能够自动判断多卡profiling是否对齐，并分析算子性能是否异常、可能是什么原因，减少人工操作。" | 自动检测多卡 profiling 是否时间对齐，多卡时间线自动叠放对比，标注算子性能异常及可能原因 | AI 判断跨卡异常算子、定位"哪张卡慢/慢在何处"并给原因假设 | 「多卡对齐，一秒对表」<br>「肉眼逐卡比对的时代结束了」 | 性能案例三（64 rank all-to-all 多卡对比，定位 rank 17/23/41 为慢卡） |
| 15 | 🟦 智能性能分析 | MindStudio 数据够，全量可视化，但缺初步分析与可优化方向，非常依赖经验，新手有门槛<br><br>原声："MindStudio数据是够的，把所有能采集的数据可视化展示，唯一就是缺少初步分析和可优化方向，非常依赖人的经验，新手上手有门槛。" | 在"全量可视化"之上叠加自动初判层，直接给出可优化方向与优先级，降低经验门槛 | AI 作为"随身性能分析师"，对采集数据自动生成初步诊断与优化建议清单 | 「数据已就绪，方向它先给」<br>「新手也有专家陪跑」 | 精度案例一~三、性能案例一~三（覆盖了"全量数据→初步诊断→可优化方向→优先级排序"的完整流程） |
| 16 | 🟦 智能性能分析 | 完全靠 AI 给结果短期不可靠；应由有经验专家把经验提炼给 AI，AI 给结果同时交出分析方法，过程与结果结合才可信<br><br>原声："如果专家完全不懂profile分析、全靠AI给结果，短期不可靠。应该是有经验专家把经验提炼给AI；AI分析时不光给结果，还把要注意的分析方法同时交给专家，过程和结果结合起来才可信。" | 构建"专家经验→规则/知识库→AI"的沉淀通路；AI 输出结果同时给出分析方法与依据，过程与结论并呈 | AI 以专家经验为底座做可解释诊断——每个结论附"我是怎么看出来的"，供专家复核 | 「经验可沉淀，结论可追问」<br>「不是替你判断，而是陪你判断」 | 全部 7 个案例（均为"专家经验→诊断路径→可解释结论"的沉淀示范，每个案例结尾附完整判据+观测手段+修改方案） |
| 17 | 🟦 训练监控·异常定位 | 算子报错不明确，plog 有信息但 Python 侧不显示；希望直接指出哪个参数 shape、违反什么约束；参数误用应直接报，避免跑完才从精度异常发现<br><br>原声："算子报错信息经常不明确。有时plog里有有效信息，但Python侧不会直接显示，用户还要自己去查plog。问题可能来自整网代码，也可能是某个shape不支持，当前报错往往不说明具体原因。如果能直接指出哪个参数的shape是什么、违反了什么约束，就能更快定位；参数使用错误也应该直接报出，避免完整跑完后才从精度异常中发现。" | 把 plog 有效信息回抛 Python 侧；报错直接指出"哪个参数 shape 是什么、违反什么约束"；参数误用在入口即报 | AI 解析底层报错+约束，生成"参数 X 的 shape=… 不满足约束 …，建议改为 …"的可读诊断 | 「报错即答案：告诉你错在哪个 shape」<br>「早一步拦下，别等跑完才痛」 | 精度案例一 §0（日志/plog诊断前置：grep plog → 提取关键行 → 翻译为"参数 X 的 shape=…，不满足约束 C，建议改为 …"的可读诊断） |
| 18 | 🟦 训练监控·异常定位 | 算子报的是 Ascend C 变量/属性名，从 torch_npu 接口用时名字对不上；不知算子设计有无命名规范<br><br>原声："算子报错时一般报的是Ascend C里边的变量名或属性名，但我从torch_npu接口去用，名字和属性得想一想才能对上，不是那么直接。不知道算子设计时有没有规范，比如两个接口的名字必须符合一定关系。" | 建立 Ascend C 内部名↔torch_npu 接口参数映射字典，报错时自动翻译到用户实际接口名；推动算子命名规范 | AI 自动对齐两侧命名语义，报错里直接用"你调用的那个参数"来表述 | 「报错说你听得懂的名字」<br>「底层变量，翻译成你的接口语言」 | 同上（案例一 §0 的 Ascend C→torch_npu 映射表：hcom_all_to_all_v_↔dist.all_to_all, aclnnSoftmaxV2↔F.softmax, aclnnMatmulV3↔F.linear 等） |
| 19 | 🟦 训练监控·异常定位 | 24 年 DFX 优化后训练报错日志仍不清楚；每次要去 Device plog grep error；希望把有效信息直接抛到训练日志<br><br>原声："从24年DFX优化开始，训练报错日志还是一样不清楚；每次要去Device侧plog grep error，感觉可以做成直接把有效信息抛到训练日志。" | 把 Device 侧 plog 的有效 error 自动抽取并抛到训练日志，免去手动 grep | AI 从海量 plog 中筛出真正相关的错误行并归纳成一句根因 | 「有效信息，主动上浮」<br>「告别 grep plog 的日子」 | 同上（案例一 §0 演示 grep plog 后直接提取关键行并翻译为可读诊断，免去手动逐行翻 Device plog） |
| 20 | 🟦 训练监控·异常定位 | HCCL/corner ops 报错看不懂；逐层找框架负责人/算子开发，链路长（两三天到一周）；没有提需求机制，找算子开发者难<br><br>原声："算子报 HCCL 通信错误或 corner ops 错误我们看不懂，只能知道框架哪块计算出错。一层层找框架负责人、算子接口或开发或看手册。定位好才提给算子开发，链路很长，短的两三天长的一周。没有提需求机制，找算子谁开发的也不太好找，算子同事不一定及时支持。" | 错误知识库解读 HCCL/corner ops 报错，给出可能层级与自查步骤；内建"一键提单/路由到算子 owner"通道，缩短定位到修复链路 | AI 翻译看不懂的错误、推断责任模块、自动匹配算子 owner 并起草问题单 | 「看不懂的错误，它替你翻译并转交」<br>「从两三天到几分钟：报错直达对的人」 | 精度案例一 §0+§3-§5（§0：HCCL timeout→翻译为 all-to-all send/recv 不匹配+softmax 输入 inf；§3-§5 逐层排查到 router FP8 overflow 根因，将 2~7 天找人链路压缩到一次 grep+查表+6 层下钻）、Infra 案例（NCCL 链路回退排查） |
| 21 | 🟦 训练监控·异常定位 | 报错那句代码不一定是原因（上游 shape/dtype/内存越界）；只能逐行打印 shape/dtype；大规模任务排队 debug 费时<br><br>原声："报错那句代码不一定是原因，可能上游算子结果 shape 不对 dtype 不一样或内存越界。从框架侧根据报错定位范围，逻辑没问题就逐行打印 shape dtype，看经过哪个算子后不对。大规模任务还要排队，debug 很费时间。" | 自动化 shape/dtype 追踪，沿数据流回溯到真正出错的上游算子，替代逐行手打；支持大规模任务离线回放定位 | AI 根据报错向上做因果推理，直接指出"真正异常发生在上游哪个算子" | 「顺流报错，逆流溯因」<br>「不再逐行打印——真凶自动浮出」 | 精度案例一 §5-§6（数值层+infra层：演示从"通信 timeout 表象"逆流追溯到"router FP8 overflow 根因"，死锁和 NaN 是同一 root cause 的两个平行后果——"报错位置≠根因位置"的完整因果推理示范） |
| 22 | 🟦 训练监控·异常定位 | 多卡多机很多卡打日志，最终报通信 timeout，实为别的进程提早退出或某算子挂；要收集几百 rank 逐个找，日志对定位无用、全凭经验<br><br>原声："多卡多机训练，很多卡打日志，最终报的可能是中间某个东西崩了，体现在通信timeout，但实际上是别的进程提早退出或某个算子挂了。要找的话可能就要收集几百个rank的数据一个个找，找得很痛苦。报的日志对问题定位没有太大用处，没办法清楚描述问题在哪里、怎么修复，完全凭经验。" | 跨 rank 事件聚合与首因排序，自动识别"谁先退出/谁先挂"，把 timeout 表象还原为最先发生的真因，免去几百 rank 手翻 | AI 在数百上千 rank 日志上做首发事件定位与根因归并，一句话说清"最先在 rank# 的算子# 出问题" | 「千卡之中，谁先倒下一目了然」<br>「timeout 只是表象，我们给你第一张多米诺」 | Infra 案例（node2 GPU 3 NVLINK lane 5 率先掉线，引发跨节点通信退化）、性能案例三（rank 17 率先过载成为 all-to-all straggler） |
| 23 | 🟨 训练监控·异常定位（竞品/对标） | NCCL 报错清晰（Unreachable/Wrong Rank），Flight Recorder 拆解通信过程；框架层给详细 Python traceback；Compute Sanitizer 精准定位非法内存/溢出/越界，两小时内闭环<br><br>原声："NCCL的报错都很清楚，Unreachable还是Wrong Rank都会给清楚的信号，Flight Recorder直接拆解通信过程；框架层面DDP信号不同步、显存越界、设备错配，直接看到非常详细的Python traceback； Compute Sanitizer 精准定位，非法内存访问、溢出、index越界也写得很清楚。基本两小时内可以完成捕捉、锁定、修复的闭环。" | 对标建设 Flight Recorder 式通信过程拆解、清晰 Python traceback、非法内存/越界/溢出精准定位，形成"捕捉—锁定—修复"闭环 | AI 串联信号→traceback→修复建议，压缩闭环时间 | 「两小时闭环：捕捉、锁定、修复」<br>「每个错误都给清晰信号」 | Infra 案例（NCCL/NVLINK 排查闭环：lane inactive 检测→CRC 错误确认→reseat/RMA→恢复验证） |
| 24 | 🟦 训练监控·精度对齐 | 精度问题最耗时，基本无专用工具，逐层 dump 跟 golden 逐层比对（torch 脚本）；定位精度问题特别困难<br><br>原声："现在花得比较多的时间是精度问题。基本没有专用工具，都是逐层dump数据，然后跟golden逐层去比对，可能用torch脚本去比。算子交付后跑端到端精度、性能测试，出现精度问题要去定位，定位精度问题是一个特别困难的事情。" | 精度对齐工作台，标准化逐层 dump 与 golden 比对流程，内建 tensor 比对与相似度计算，替代零散脚本 | AI 自动选层 dump、自动比对并定位首个分叉层，给出可疑算子 | 「精度定位，从'炼丹'到'流水线'」<br>「逐层对齐，一键找到分叉点」 | 精度案例三（逐层对比 BF16 baseline，绘制误差累积曲线，锁定 layer 35 为首个分叉层） |
| 25 | 🟦 训练监控·精度对齐 | 易出在并行切分不对/融合算子不满足约束，需多迭代由粗到细；打印后还要识别到哪行/哪个算子，再自写工具做 tensor 对比算相似度；希望可视化整网+对应代码+点选节点自动 dump+可视化对比 golden 差异<br><br>原声："比较容易出现在并行切分不对，或有些融合算子不满足约束条件，可能需要几个迭代，一开始打得粗一点再打到细节点，打印完还要识别到哪一行代码或哪个算子有问题；打印出来之后还要自己写工具做tensor对比、算矩阵相似度。如果能直接可视化整网模型、对应到代码，点选节点自动加dump，再可视化对比golden差异，会方便很多。" | 可视化整网模型并对应到代码，点选节点即自动插桩 dump，可视化对比与 golden 的差异；内建并行切分/融合约束校验 | AI 依约束库自动排查"切分不对/融合不满足约束"，并推荐 dump 粒度（先粗后细） | 「点哪一层，dump 哪一层，比哪一层」<br>「模型即画布，差异自动上色」 | 精度案例一 §3（通信调度层：EP=64 切分校验——256 experts/64 ranks 名义均匀，实际 router 塌缩致 1 rank 承载 100% token，演示并行切分不匹配的诊断方法）、精度案例二（q_b_proj 融合算子 FP8 约束不满足） |
| 26 | 🟦 训练监控·精度对齐 | 采集 NPU/GPU 生成 Excel 比对、差异大标红，但还要在代码里打点自比；标红处不一定是精度产生处，异常可能在标红前，要一步步往前定位；智谱迁移+精度花了两个月<br><br>原声："工具采集NPU和GPU数据生成Excel比对，差异大会标红，但标出来后还要在代码里打点打印张量自己比对；标红的地方不一定是精度产生处，异常可能出现在标红之前，要一步一步往前定位。智谱的问题光迁移和精度定位就花了两个月，后一个多月用各种工具对齐仍不完美。整个工具我都用了一遍，每个多少有点作用，但用工具找到问题的过程比学会用工具要长很多。" | 差异传播视图，不只标红当前点，而是沿计算链回溯到"误差最早产生处"，免去代码里手动打点 | AI 判断标红是否真源头、自动向前追溯到误差起点，把两个月的对齐压缩到可控范围 | 「标红只是终点，我们带你找到起点」<br>「误差溯源，一路向前」 | 精度案例三（误差传递因果链：从 layer 35 沿 q_b_proj→core_attention→gate_up→残差连接逐级追踪，找到 layer 35 为误差起点） |
| 27 | 🟦 训练监控·精度对齐 | 希望从上到下把推理线到训练线拿出来，对同一 token 为什么概率不同；要的是调用链（每个函数/推理接口/改了哪层），不只模型结构图；还想拿正常 token 跟异常 token 比看哪层输出差异大<br><br>原声："我希望从上面有个工具能打开看，推理这条线到训练这条线，对同一个token为什么概率不一样；现在基本靠人工，没有一个比较好的工具能从上到下把两边拿出来，一步一步定位到比较小的问题范围。不能只有模型结构图，我要的是调用链，我调的每一个函数、推理哪个接口、接口里又调了什么、更改了哪一层；还可以拿正常token跟不说人话的token比，看哪一层输出差异大。" | 训推一体调用链视图，展开每个函数/接口/层的调用与改写，支持推理线 vs 训练线、正常 token vs 异常 token 的逐层输出对比 | AI 对比两条链路逐层输出，定位"哪一层开始差异变大"并解释可能原因 | 「不止模型结构，更是完整调用链」<br>「同一个 token，两条路一层层对齐」 | 精度案例三（扩展：训推一体逐层对比——同 token 在推理/训练两条链路逐层 diff） |
| 28 | 🟦 训练监控·精度对齐 | 低精度训练画 loss/z-loss/load balance loss/lr/grad norm/吞吐；某些格式收敛有问题（BF16/MXFP8 正常、FP8 不行）；需插件分析量化误差传播过程与链路<br><br>原声："我们会把低精度训练任务的监控数据画出来，看loss、z-loss、load balance loss、learning rate、grad norm和吞吐。有些格式下训练收敛会出现问题，比如BF16和MXFP8正常，FP8不行。要定位从哪个位置开始出现较大跳变或引入误差，需要一个工具插件分析量化误差的传播过程和传播链路。" | 低精度训练监控面板（上述指标），叠加量化误差传播分析插件，定位从哪层/哪步开始跳变或引入误差 | AI 分析不同格式（BF16/MXFP8/FP8）下的误差传播链路，定位首个显著引入误差处并给建议 | 「让量化误差的传播看得见」<br>「FP8 为何不收敛？顺着误差链一路问到底」 | 精度案例三（HiF8 vs BF16 低精度训练退化——loss/z-loss/grad norm 监控 + 量化误差传播链路分析）、精度案例一 §5（数值层：AMP scaler 衰减曲线(65536→4096)+ z-loss 缺失分析+ router logits 分布，给出 FP8 低精度训练退化链路和观测方法） |
| 29 | 🟦 训练监控·精度对齐 | 数值分析难、问题不稳定复现、数据量大；自写 Python 按 bin 画分布曲线、提 kurtosis 等指标，放 Jupyter 可视化；尚未形成服务/产品功能<br><br>原声："数值分析很难，问题不一定能稳定复现，数据量又非常大。我们要分析张量的数值分布，自己用Python脚本按bin画分布曲线、提取kurtosis等指标，再放到Jupyter notebook里可视化；现在还没有形成服务型或产品型功能。" | 把张量数值分布分析产品化：自动按 bin 画分布、算 kurtosis 等指标，内建可视化，替代零散 Python+Jupyter | AI 在海量、难复现的数据上自动发现异常分布/离群指标并提示关注点 | 「张量分布，开箱即分析」<br>「难复现的问题，交给会盯梢的 AI」 | 精度案例三 §3.1~3.3（分布曲线+KDE密度图、kurtosis/Skewness/Outlier Ratio/KL Divergence/QSNR 等宏观指标、量化风险评估矩阵——完整的产品化示范） |
| 30 | 🟨 训练监控·精度对齐（竞品/对标） | TensorBoard 监测偏离超阈值即局部熔断，两端加载相同权重/输入强制前反向，用 Tensor Hook 非侵入拦截每层输出，逐层比余弦相似度/绝对误差，<0.999 锁定分叉层<br><br>原声："TensorBoard监测到国产卡loss曲线跟NV偏离度超过阈值就局部熔断；然后在测试床上两端加载完全相同的权重跟输入，强制执行前向和反向传播；用自研Tensor Hook注入脚本，不破坏原有计算图，自动拦截每一层Transformer layer的输出；再逐层自动化比对两端tensor的余弦相似度和绝对误差，相似度低于0.999就锁定精度分叉在哪一层。" | 对标：偏离阈值熔断+同权重同输入强制复算+Tensor Hook 非侵入逐层拦截+逐层余弦/绝对误差比对（<0.999 锁定分叉层） | AI 自动设阈值、编排双端复算与逐层比对，自动锁定精度分叉层 | 「相似度跌破 0.999，立刻锁定分叉层」<br>「不改计算图，逐层对齐两端」 | 精度案例一 §5.5（熔断/预警层：AMP scaler 65536→4096 三级预警 🟡注意/🟠警告/🔴熔断，step 15202 时若触发熔断可避免 NaN + 复盘时间线 + 万卡成本核算）、精度案例三（KL Divergence > 2 bits 可作为熔断阈值） |
| 31 | 🟨 训练监控·精度对齐（竞品/对标） | 上线前核对训推混合精度/loss scaling/归一化/激活算子、是否统一 TE 算子/并行分片/NCCL 同步，锁版本同镜像；线上 NVTX 埋点定时导权重与特征快照，漂移后分层定位<br><br>原声："上线前先核对训推两端混合精度、loss scaling、归一化、激活算子，校验是否统一用TE算子、并行分片和NCCL同步；再锁版本、保证训推同镜像。线上开NVTX张量埋点，定时导权重和中间特征快照；发生漂移后按单卡、多卡、算子、并行、数据链路分层定位。" | 上线前一致性核对清单+锁版本同镜像；线上 NVTX 埋点定时导权重/特征快照，漂移后按单卡/多卡/算子/并行/数据分层定位 | AI 自动执行一致性核对与漂移分层定位，给出最可能的链路 | 「上线前先对表，线上再留快照」<br>「漂移一发生，分层定位即启动」 | —（竞品对标需求，暂不设案例） |
| 32 | 🟦 训练监控·任务与资源管理 | 报错了服务不一定停，一直卡着跑日志，找错误要往上翻很久；程序报错应停掉，别占资源让人排队<br><br>原声："报错了服务不一定停，一直卡着跑日志，找错误要往上翻很久。程序报错应该停掉，不然占着资源别人排队。" | 训练异常自动熔断/停止策略，报错即释放资源并保留现场，避免空跑占卡与日志堆积 | AI 判定"致命错误还是可恢复告警"，致命即停、可恢复则告警续跑 | 「错了就停，别占着卡位」<br>「智能熔断：该停则停，该扛则扛」 | 精度案例一 §5.5（熔断/预警层：🔴熔断级(loss=NaN 或 scaler<1/32)→立即停训+释放 GPU 资源+保存 checkpoint，解决"报错不停、占卡位"痛点） |
| 33 | 🟦 训练监控·infra | 512 rank 某节点错误经 gather 扩散到所有机器，不可能逐一比较；只能在代码里加打印写 Python 做逻辑比较；现有诊断平台诊断不到这么细<br><br>原声："512个rank某节点错误通过gather扩散到所有机器，不可能每台逐一比较，我们在代码里加打印写Python做逻辑比较。比如100个rank第8个节点最先出问题，现有诊断平台也诊断不到这么细；当前团队都需要手动按流程排查。" | 细粒度跨 rank 逻辑一致性比较内建化，自动定位"哪个 rank 的哪个节点最先出错"，替代手写打印 | AI 在 rank 间做自动一致性比对与首错定位，精度细到单节点 | 「扩散的错误，追回它的震中」<br>「第 8 个节点最先出错——它自己会说」 | Infra 案例（node2 GPU 3 率先出错→NVLINK lane 5 inactive→扩散到跨节点 IB 负载异常）、精度案例一 §6（infra层：5 步错误扩散链路——rank 23 单点溢出→all-to-all barrier→64 rank 全卡→PP pipeline 断裂→NCCL timeout 报错，演示"谁先出问题"的自动定位） |
| 34 | 🟦 训练监控·infra | 万卡训练突然 NaN/loss 跑飞/吞吐骤降，希望实时识别、定位根因、给方案；万卡两块钱一卡时即两万一小时，十二小时搞不定就是二十四万<br><br>原声："几百B、近万亿参数的万卡训练，突然出现NaN、loss跑飞或吞吐掉得很厉害，如果能实时识别、定位根因、给出解决方案就很有用。万卡集群两块钱一卡时就是两万元一小时，十二小时搞不定就是二十四万没了。" | 实时训练健康监控+NaN/loss 跑飞/吞吐骤降的在线检测、根因定位与处置建议，最大限度止损 | AI 实时识别异常、秒级定位根因并给出解决方案，把损失窗口压到最小 | 「万卡训练的实时黑匣子」<br>「每一分钟都是钱——秒级止损」 | 精度案例一 §8（止损链路总览：step 15203 loss NaN→plog 翻译→分叉判定→通信调度层→模型层→数值层→熔断预警→扩散分析→7 项修复+验证，完整 8 阶段止损链路 + 时间估算：无工具 2~7 天→有定位链 ~30 分钟） |
| 35 | 🟦 训练监控·infra | 要比对几千卡：哪个时间点哪张卡先出问题、慢在什么阶段，人工不可行；已做工具把时间片打出来，128/1000 节点微秒级分析谁最先慢、慢在通信还是计算，客户云控制台自助部署查看<br><br>原声："我们要比对几千卡，到底哪个时间点上哪个卡发生了问题、谁先发生的、慢在什么阶段，靠人工基本上不太可行，所以做了这个工具，把时间片都打出来。可以把128个节点、1000个节点输进去分析，分析到微秒级，到底谁最先慢、慢在通信还是计算；客户在云控制台网页上就可以安装部署、查看。" | 强化千卡级时间片分析，微秒级判定"谁最先慢、慢在通信还是计算"，支持云控制台自助部署查看 | AI 在千/万卡时间片上自动找首慢者与慢因，一键出结论 | 「千卡竞速，谁先掉队一眼看穿」<br>「微秒级裁判：慢在通信还是计算，当场判定」 | 性能案例三（64 rank all-to-all transit/wait 瀑布图，微秒级判定 rank 17/23/41 为 all-to-all straggler——慢在通信，根因是 router 负载倾斜） |

