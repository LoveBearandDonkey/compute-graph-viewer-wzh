
# 训练“冷”知识

### 模型是什么

简单说，大模型，背后的样子，就是一堆数量恐怖的数字。

### 我们一直在和“数字”对话吗？

我们给大模型输入的文字、图片、视频、气象信息，最终也是转化成了数字，大模型输出的，也是一堆数字，再转换成了回答、图片、视频、气象预测……

### 这么聪明的模型，这得是多大规模的数字

以 openPangu-2.0-Flash 为例，它是（约）92B模型，即920亿个BF16标量参数（组成权重张量），每个BF16参数2字节（16位）算，一共1840亿字节，即裸参数大小就184GB。

模型除了参数，还包括训练前为了形成这套参数而设定框架、计算方法等，预计到了200GB，几乎塞满256 GB的HUAWEI Pura 90 Pro Max的内存。

### 市面不同模型的差异就是这堆数字的差异吗？

模型的核心竞争力确实主要在参数，但框架、计算方法的设定，也是参数推理运转时脱离不了的专属配套基座。虽然结构框架和计算方法基本是业界常见的方法。



>什么是BF16
>BF16一度是最常见数字“尺寸”，16个二进制位保存一个浮点数。千万不要理解成它是要求固定“小数点后几位”，而是16位存储空间映射十进制数字，约2～3位（log10(2⁸) ≈ 2.4）有效数字。
>|原始数值|转成BF16后|规律|
>| | | |
>|1.234567|1.23|3个“有效数字”|
>|1234.56|1236|4个“有效数字”|
>|0.00123456|0.00124|3个“有效数字”|
>
>BF16，数字上下限是10的正负38次幂数量级，但实际权重往往集中在0附近。
>拓展，低精训练的INT8，其实就是整数


如果为了感受规模，假想全部排成 2560×2560 的方阵，大约相当于 1.4万个矩阵，不过真实模型中，不同矩阵承担不同工作，例如理解词语、传递信息、选择专家和生成下一个词。

> ** 2560 **
>2560来自openPangu-2.0-Flash的隐藏维度.hidden_size = 2560。也就是说，模型内部通常会把每个token表示成一个长度为2560的向量。
>比如“苹果” 这个token就是→ [0.13, -0.82, 0.04, ...]  共2560个数。
>但openPangu的权重绝不都是2560×2560。2560只是主要大小，核心隐藏维度。

> 1.4 亿
>2.0flash一共46+3层，怎么有1.4亿个矩阵？
>举例，46层有44个MoE层，中随便一个MoE层的256个专家，就有Gate、Up、Down3个逻辑权重矩阵，这里就3.3万+矩阵了。还有其他更多功能主题的矩阵……

### 一次对话，得算多少内容？

数字的恐怖不仅是体量，还包括背后的计算量。比如一句话中的一个token“苹果”，仅在44MoE层中的1层，就要在256中选topK，即8个MoE专家处理。整个模型累计下来，就要激活约12GB的参数参与计算。

一个参数，只做一次线性计算即一次乘法 + 一次加法≈ 2次浮点运算。一个token，穿过一个模型，约有120亿次浮点运算。

所以聊一次天，比如问一个100汉字的问题：
>我周末准备带父母去杭州玩两天，住在西湖附近。父母六十多岁，不适合走太多路，预算3000元，希望看看西湖、灵隐寺，吃一些本地菜。请帮我安排路线、交通和吃饭建议。

加入输入输出合计处理约1000 tokens的话，就有12万亿次浮点运算，加上注意力和其他操作。这个通常由多张NPU花费几秒完成。



这些数字一开始是随机的，然后要这堆数字根据我们输入的问题猜我们输入的对应问题的答案。第一次，它非常不准。但它可能陆续猜，关键是每次根据猜测结果去对比真实结果，反过来一点点迭代上一次的数字，

这些参数不是平铺瞎凑一堆，它们被组织成许多尺寸不同的数字矩阵。


在这个过程中，这些数值的变化和组合搭配，就神奇的存储了“知识”，让模型能够预测（推理）内容。

当第n次学习，数字已经变化不大的时候，就可以领毕业证，可以出师实战了。一开始那批随机的数字，变成有特定的数值。

这个数值变形过程是不可控的，同样的语料，再训一次，数字搭配一定不一样，但一定能神奇的进行“预测（推理）”
数字能存知识，做推理，一点都不抽象，对比我们大脑中的记忆和思考，我们大脑的“原料”连数字都是，更抽象

>我理解的训练过程
>一幅漫画插画，是3人在这NPU站台上玩传话游戏，一句原话，被传得面目全非，有个CPU大哥在终点对比传过来的话和原话，给每个人提了建议，优化器把建议告诉每一个人。下一次传话，变得更精准了

## 数据规格

综上，训练、推理时的输入张量[Batch Size, Sequence Length, Hidden Size]如[8, 4096, 2560]，表示一批次处理8条文本，每条文本4096个token，每个token用2560个数表示。

训练时，语料会切满“8”x“4096”那么规整

推理时，语料肯定没有设想的“8”x“4096”那么规整

为了提高GPU/NPU利用率，推理服务还经常把不同用户的请求放进同一个Batch计算，不同用户问的问题可能被拼在一起输给模型。但不会把它们当成同一段上下文，所以不会相互干扰。

拼长度：文本token不够4096没关系，按实际Sequence来。通常会填占位的padding，补齐到这一批最长token长度，形成规整矩形张量即可，比如[3, 7, 2560]

拼batch：等待并拼凑多个不同的请求。另外实在凑不够也不影响推理，硬件利用率低一点而已。如果文本超过4096可以分块后拼凑处理、滑动后拼凑处理、RAG检索有用的局部输入模型等

激活，残差等见：Transformer结构与并行策略知识库的145行

#### 有时和AI聊着聊着就提示接近上下文窗口限制了。这个和每次对话的seq有什么关系？

上下文窗口限制，就是模型当前这一次推理所允许的最大有效序列长度S_max。

openPangu2.0flash为例
序列长度4096，最大上下文512k（512K = 512 × 1024 = 524288 tokens）。如果每次对话消耗 1000 tokens，预计可以聊500轮。

这里的最大有效序列长度只指单次申请，而不是累计。比如聊到第10轮，S为1000+9*1000=10000 < 524288，放行！

### 从小到大

#### token

token：是分词器词表中的一个编号，不一定只有一个字。每个模型有自己的切分规则。

一个token有多大，和隐藏维度有关，比如openpangu2.0flash的H=2560，如果使用BF16。每个数占2字节，2560 × 2 = 5120字节 ≈ 5KB

#### Embeding 权重表

类似一个人掌握的词汇量和对词汇的理解。openPangu的词权重表大小是15万，形状是[151552, 2560]，一个token一个坑，一个token（比如“喜欢”）就是151552行中的某一行，一个长度 2560 的一维向量，即隐藏维度。这2560个数字最终表示了单个token的实质“含义”。隐藏维度越大，理论上单个token可以存储的信息量更丰富。

>注：分词器也叫词表，负责拆token，这个表随着训练进行是不会动的。

每个token进入模型后，会先在Embedding层查出自己的2560个数字，这个数字在每一层都会被计算一次。用新的结果进入下一层。

每个step的最终，反向阶段传回的梯度，也有emb的部分，更新这个词表中本step涉及到的token的2560数字。

序列长度：一次xx处理多少token，常见4096。

>模型中的张量不一定都是2维的
>比如：
>Norm权重：1维
>Linear权重、Embedding权重：2维
>注意力激活：常见4维

>常见问题1：Tokenizer实际编码  遇到苹果时能切“苹”“果”，遇到“苹果手机”时，切“苹果”“手机”，它怎么知道苹果要单切还是切一起？
>有固定算法，但是不一定精准

>常见问题2：遇到“苹果手机”时，切“苹果”“手机”，遇到苹果派时切“苹果”“派”。虽然都切成“苹果”但含义不一样，这两个含义的苹果在词表都是同一套隐藏维度矩阵吗？
>同是“苹果”，查出来的Embedding的[2560 × 2560]数字编码是一样的，他们语义的差异关键在训、推时和其他词形成的上下文，产生不同的隐藏状态表达不同含义。Embedding保存的是“苹果”的通用起点，具体语义主要由多层注意力和上下文共同形成。

>常见问题3：训练会优化词表的embeding吗？

## 模型结构

openpangu为什么不全是MoE层，非要在前面加2层Dense层？因为可以让进入模型的token经过Dense稠密FFN后更有初始上下文信息，更有区分度的进入专家区

## 并行切分

Total Routed Expert=256，EP=64。不是说要把4个专家分一组，而是切分了EP份后，只能4个专家在一组。这个因果关系理解反了差异会很大。

正交和切出，每rank专家数都是 = Experts ÷ EP。因为“正交”和“切出”不会改变一套专家内部的EP切分方式。

区别在于

EP切出（主流）
就是EP切片和DP的切片会一起混放到Rank上。DP和EP必须呈倍数关系，且DP必须大于等于EP。DP/EP等于多少，就一共多少套专家。
可以理解为比如512个DP分片商量，每64个DP分片拼团一套专家，那一个模型一共8套专家。
每一个DP所在的Rank上虽然专家不全，但是可以跨DP共用其他DP的专家，形成一个EDP组，一个EDP组有一套完整的专家，一共8个EDP组。
同样的rank数下，DP可以自然切更多，因为他不需要EP正交那样疯狂复制自己（去迎合EP切分）而占了很多rank。
最终DPxPP=Total Rank

EP正交
因为完整专家被切了64份，所以每一个DP还需要【重复】复制自己64份（很占Rank），给64套专家分片去计算，64份计算结果合成一套完整专家计算结果，不然覆盖不全专家。
这种方式下，DP间专家隔离。一共有几个DP，一个模型就有几套专家。
同样的rank数下，DP不得不更小，不然DP重复自己后很难放得下，DPxPPxEP=Total Rank

Layer角度
Layer间专家隔离，Layer必须有一套完整专家。Layer自然要靠一个Stage所有Rank支撑起这一套专家。
一个 layer 横跨一个 stage 所有 rank。
一个 rank 会承载一个 stage 所有 layer 的部分或全部权重（比如router / norm 的权重无论怎么切分，恒为完整拷贝）
进一步说
Stage1的L12~L23一共12层layer，每个layer都会横跨Stage1的所有Rank
如果要设立4个Stage，那就要4倍的Rank
所以Total Rank的计算要 xPP

一个layer有没有一套完整的专家？
每个layer有一套完整专家，那moe x 44层理论上就有44套完整专家。但EP切出一共8套完整专家，EP正交一共也8套完整专家。44和8就对不上？
这里理解有误，首先，这个模型真正有44套专家（44套专家权重）
正交和切出中的DP8，和EDP8，指的是在一个layer下，8套专家在8个DP\EDP同时工作
一个rank下的moe token，它可能来自8个dp，假如他来自第一个dp，那它all to all 时，会和stage内所有rank通信一遍