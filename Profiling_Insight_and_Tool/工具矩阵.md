# 工具矩阵

## 训练管理与监控

| 工具名 | 路径 | 功能 |
| --- | --- | --- |
| Training Run Explainable Twin（训练运行可解释孪生；`launch-v2.html` 里显示为「训练任务监控 · 监控主页」） | `Profiling_Insight_and_Tool/training-run-twin-standalone/training-monitoring-v2.html` | 训练监控大盘：整网 3D deck 正视图、集群热力矩阵、MoE 专家负载（Router 倾斜）等实时监控卡片；「问题诊断」卡片联动左侧架构图高亮，配「聚光灯定位链」逐层举证（路由倾斜、显存 OOM 等典型故障场景），并提供「时光全景」浮窗回看整段训练关键事件。 |
| Config & Relation Observer（配置与关系观测台） | `Profiling_Insight_and_Tool/training-run-twin-standalone/config-relation-observer.html` | 四域配置仿真与关系观测：整网 / Model Architecture / MoE / Cluster 四区联动配置（Layer、DP/PP/TP/CP、专家数、集群规模等），支持一个 step 数据流播放、单卡显存容量测算(等距投影图 + 越界判定)；运行事件模式下切换为传播源 → 受影响域的溯源画布，并可查看关联的计算血缘。 |
| Config & Relation Observer · 平面视图 | `Profiling_Insight_and_Tool/training-run-twin-standalone/config-relation-plane.html` | 配置与关系观测台的平面视图变体：与 `config-relation-observer.html` 共用同一套配置域与规则层，布局重排为「左栏 / 中无限画布 / 右详情」三栏。左栏顶部是一对互斥页签「训练配置 / 整网查询」：训练配置 = 配置预设 + 「表单 / 代码」两档（表单按 Model Architecture / Cluster / MoE 三区拨参数，代码档由当前配置实时生成 YAML，文件名即样例配置切换口，可从 `config-test/` 的公开配置里挑一份，右栏按字段中文名逐项列出该份配置的实际取值并给出与表单的兼容度百分比）；整网查询顶掉预设与两档，只留整网 3D deck。中间画布把四域（整网 / Model Architecture / MoE / Cluster）以平面矩阵铺开，便于观察并行维度与配置项之间的关系；支持配置导入/导出、单卡容量与各 PP Stage 峰值显存测算。 |
| 训练管理平台 · 任务对比 | `Profiling_Insight_and_Tool/training-run-twin-standalone/TaskCompare.html` | 多任务横向对比：左侧任务勾选列表（含基线标记），右侧指标折线 / 看板对比不同训练任务（run）的表现差异，用于挑选最优任务或定位退化任务。 |
| Rank 的设计表达 · 分步入门（`config-relation-observer.html` / `config-relation-plane.html` 右上角「what is rank」入口） | `Profiling_Insight_and_Tool/rank-intro/rank-intro.html`（原 `training-run-twin-standalone/rank-intro.html`，2026-09-20 迁出为独立目录，附带 `vendor/` 下的 model-architecture-3d-deck / model-graphviz 两个 pattern 副本） | 两个页签。**「Rank 的设计表达」**：面向小白的 rank 概念十步入门，单张 canvas 上从「一张卡 → 一台机器 8 rank → 16 机 128 rank 集群」逐步变形，再依次叠加 PP 按层切段、DP 复制、TP 劈权重、EP 专家分桶四个并行维度，接着往框里装显存五档（权重 / 梯度 / 优化器 / 激活 / 预留）看溢出，演示训练时一个 rank 沿各并行轴跟谁通信，最后展平成简易观测格子；左栏读出卡里各档 / 权重块可点开看二级读数与对应通信五元组，配置口径与 Config & Relation Observer 的 default128 一致。**「实战场景」**：四个分段动画案例，每段一枚黑标圈出图上对应区域 —— ① 一个 Rank 出错四步拖停整个集群（R119 layer 38 溢出 → EP 组卡 barrier → 模型副本流水僵住 → 全集群超时抛错，对应定位链「问题影响 2」all-to-all 死锁）；② 集群状态热力全景（计算时间 / 空闲时间两种视图互补，整机横带 = 硬件降频）；③ Rank 内存的动态变化监控（开机 / 正向 / 反向 / 更新四阶段各档涨落，第 5 步 Layer 7 热点专家只让 stage 0 四张卡 OOM，右列「选中 Rank 详情」可关闭，内嵌微批次生命周期泳道快照）；④ 配置预览：改一个数，矩阵、Layer × Rank 计算图与显存账一起变，参数校验失败时给出「合法且装得下」的建议修法（先满足整除关系再过容量关，横幅标注预计峰值 GB/卡）。 |
| Router Logits 张量计算可视化 · openPangu-2.0-Flash | `Profiling_Insight_and_Tool/training-run-twin-standalone/router-logits-viz.html` | MoE Router 数值溢出的张量级讲解：x · W_gate → logits → FP8 E4M3 cast → softmax → probs 四张量联动（x 按行、W_gate 按列、logits / probs 按格选中），公式区逐项展开被选格的点积与 softmax 计算；三情景切换（step ~10000 正常态 / 15200 塌缩前夕 / 15203 溢出）复现「193 列范数 2 → 50、token 0 与该列对齐 → logit 1846 撞 448 上界 → 整行概率 NaN」的定位链案例，配 max\|logit\| 天花板条。 |
| MindSpeed × Profiling Insight · 双向能力与 UX 机会地图（`config-relation-observer.html` / `config-relation-plane.html` 右上角「MindSpeed intro」入口） | `Profiling_Insight_and_Tool/training-run-twin-standalone/mindspeed-tool-matrix-analysis.html` | MindSpeed Core（昇腾 Megatron-LM 训练加速库）与本工具矩阵结合的分析长页：先用用户问题墙讲「没有 MindSpeed 时训练工程师卡在哪」，再拆解仓库的并行编排 / 显存治理 / 通算优化 / 计算加速四维能力与两种典型用法，给出它能充实的服务场景、与现有工具（Config & Relation Observer、任务对比、Twin 等）的结合优先级，以及反向可为 MindSpeed 做的 UX；落点是首个原型「寻优驾驶舱」（配置搜索空间剪枝 → 预测 → 验证收益的闭环）与三阶段落地路线。 |

## 性能分析

| 工具名 | 路径 | 功能 |
| --- | --- | --- |
| 性能分析（MindStudioNext v3） | `Profiling_Insight_and_Tool/AI_Profiling_Tool/index_v3.html` | 综合性能分析工作台：计算图 / openPangu 整网图可视化、HBM 显存快照、多机多卡 step-trace Timeline 泳道、MFU 分析等多页签集成入口，是本目录下功能面最广的分析主入口。 |
| 训练性能分析报告 | `Profiling_Insight_and_Tool/AI_Profiling_Tool/report.html` | 性能分析报告形态：复用「性能分析（MindStudioNext v3）」的报告数据与图表，渲染为固定浅色、A4 文档流版式的可打印/导出 PDF 报告，供转发与存档，与工具形态互为入口（页面顶部「← 工具形态」可跳回 index_v3.html）。 |
| PTO 性能分析 · 任务时间对比 V2 | `Profiling_Insight_and_Tool/AI_Profiling_Tool/profileCompare.html` | 泳道对比查看器：以 Mix 子图核间同步为主题，构造同步竞争、调度空泡、FFN 流水失衡三类典型异常 run，双栏泳道对比展示任务条、等待段、同步弧线，用于性能问题模式的稳定演示与教学。 |
| HiF8 精度诊断工作台 · Ascend A5 | `Profiling_Insight_and_Tool/training-run-twin-standalone/hif8-precision-workbench-V3.html` | 低精度量化（HiF8/FP8）精度诊断：数值分布（原始 vs 量化后）、动态范围利用率、溢出率、量化 SNR、分布形态适配度等多维度分析，支持训练步回放观察量化误差的传播与累积。 |

## 训练可视化（pangu-moe-trainviz）

| 工具名 | 路径 | 功能 |
| --- | --- | --- |
| openPangu-Ultra-MoE-718B · 算子 × 硬件 × 时间（wzh 分支版） | `pangu-moe-trainviz/op-rank-time-wzhBranch.html` | `op-rank-time.html` 的个人分支版本：以 openPangu-Ultra-MoE-718B-V1.1 真实架构数据为底，在 3D 场景（正视 / 侧视 / 轴测切换）中把模型层、算子、数据流、通信（TP All-Reduce、EP All-to-All、PP Send/Recv、Logits Gather）与硬件 rank 映射到同一坐标系，配 DP2 × PP2 × TP8 的 1F1B chunk-level 调度泳道、step 回放控制、对象聚焦面板与「诊断 / 聚焦」inspector，并叠加训练指标时序与 Loss 趋势图。 |

> 说明：以上 12 个工具均为纯前端单文件页面（部分依赖同目录 `css/` `js/` `vendor/`、`pangu-moe-trainviz/data/` 与仓库根级 `vendor/pto-design-system`），需通过本地 HTTP 服务打开，不能直接以 `file://` 方式访问。

---

# 文档索引

范围：`Profiling_Insight_and_Tool/` 下**本项目自建**的说明/知识类 md 文档；不含 `AscendProfKit/`、`skills/`（含 `swimlane-skill-pack/skills/`）等第三方技能包内部文档（agent prompts、API 参考、SKILL.md 等），也不含 `Analysis Report/` 下的历次 profiling 分析报告。

## 顶层项目文档

| 文档名 | 路径 | 主要内容 |
| --- | --- | --- |
| 目录结构与更新日志 | `Profiling_Insight_and_Tool/README.md` | 目录结构说明 + 反向时间顺序的关键更新日志（AI 辅助性能调优工具、Issue_PDF_analysis 各文档等），部分目录描述已滞后于当前实际结构。 |
| 950data 项目知识结构 | `Profiling_Insight_and_Tool/KNOWLEDGE.md` | 把昇腾 950/CANN 技术峰会 24 份 PDF + 3 份 Gitcode issue 导出 CSV 整理成结构化知识地图：硬件架构、CANN 软件栈、算子编程、数据精度、大模型实践、性能调优、优化案例、Issue 仓库等一级主题，每条知识点标注来源文件。 |
| 模块说明（给其他 Agent） | `Profiling_Insight_and_Tool/AGENTS.md` | 告知 Agent 本目录是 PTO 工作区的性能分析产品区、非独立项目；给出完整项目根目录、常用入口页面、本地 HTTP 启动方式、共享依赖（设计系统、AscendProfKit）与协作规则。 |
| 计算图 Profiling 证据工作台 PRD | `Profiling_Insight_and_Tool/COMPUTE_GRAPH_PROFILING_PRD.md` | `graph-evidence-workbench.html` 的产品需求文档 v0.1 草案：一句话定义、MVP 范围（P0 仅支持 Qwen2-7B、需解析真实 trace_view.json、数据外置为 JSON）、官方工具链分析路径调研依据等。 |
| 需求解决方案总览表 | `Profiling_Insight_and_Tool/CheckList.md` | 用户访谈原声整理成的大表：35 条按算子调优/通信/内存/整网/精度对齐/训练监控/infra 等分类的用户问题，逐条给出解决方案、结合 AI 的解决方案、宣传 slogan 及对应的定位链演示案例。 |
| GPT 问答总结 · config 待办 | `Profiling_Insight_and_Tool/GPT问答总结-config待办.md` | 四轮 ChatGPT 问答的整理稿，作为配置/通信观测功能的待办依据：①MoE layer 层内/层外通信清单（TP/CP/EP/DP 各 collective 的阶段、传输内容、通信范围，含常见配置下的通信清单）；②DeepSeek-V4 × PyPTO × Ascend 的 Model-to-NPU 训练工程论述（Execution-centric、State-first、Data Ownership、Training Execution IR、五张核心观测图与可观测数据模型）；③大模型训练 Rank 状态观测的正确性校验与推荐 Rank State Map；④TP / MoE-EP / Rank 数据流的通信观测建议（统一 Communication Event 模型、Routing Graph、四类通信视图、最小 Telemetry Schema）。 |

## training-run-twin-standalone/ 文档

| 文档名 | 路径 | 主要内容 |
| --- | --- | --- |
| openPangu-2.0-Flash 架构参考 | `Profiling_Insight_and_Tool/training-run-twin-standalone/openPangu-2.0-Flash架构参考.md` | openPangu-2.0-Flash（Sparse MLA + DSA/SWA + 256-Expert MoE）模型架构规格文档：46 层 decoder 骨架、注意力/FFN 混合类型、各投影维度，数据来源标注为源码/config 校验的 canonical schema，含未核实量的「待补」标记。 |
| 训练精度问题定位链 | `Profiling_Insight_and_Tool/training-run-twin-standalone/定位链-openPangu-2.0-Flash.md` | 精度异常从表象到根因的分层定位方法论：迭代层→（通信调度层/模型层）→算子层→张量层→infra 层→熔断预警层→超参代码层，每层给出定位目标、观测手段、判据、产出，是训练监控/HiF8 精度诊断工作台等页面「定位链」功能的设计依据。 |
| 页面构建提示词与业务笔记 | `Profiling_Insight_and_Tool/training-run-twin-standalone/prompt.md` | 混合型笔记：要求页面遵循 `pto-design-system` 的 SKILL.md/DESIGN.md 与 `ide-frame` 框架、优先复用现有 pattern；另附「性能对比业务」分析（可比性门禁→汇总数字→分类 Top-N 表→泳道，泳道对比的不可替代价值），是 TaskCompare/profileCompare 类对比页的需求来源。 |
| MC2 算子异常诊断报告 | `Profiling_Insight_and_Tool/training-run-twin-standalone/history.md` | 一份具体故障复盘（文件名与内容不符，实为诊断报告非历史记录）：vLLM + Ascend HCF 推理在 MC2 通信融合算子（AllGatherMatmulV2/AlltoAllMatmul）上反复崩溃 EZ9999，4 次复现证据链，根因为多个 graph 共享同一 `aclOpExecutor` 导致 CCU mission 资源污染。 |
| 模型推理相关访谈洞察 | `Profiling_Insight_and_Tool/training-run-twin-standalone/模型推理相关访谈洞察.md` | 对盘古/GPU 专家访谈信息表（1,535 条信息点）中 391 条推理相关条目的分析总结：推理端到端工作流与关键断点、六条核心结论（推理优化是系统工程、精度问题比性能更易阻塞发版、缺跨方案自动对比、NPU 成本集中在算子/文档/报错、RL 把推理变成训练主链路、最佳产品形态是统一诊断工作台）。 |

## AI_Profiling_Tool/ 文档

| 文档名 | 路径 | 主要内容 |
| --- | --- | --- |
| 报告形态竞品分析与策划 | `Profiling_Insight_and_Tool/AI_Profiling_Tool/REPORT-FORM-PLAN.md` | `report.html`（报告形态）相对 `index_v3.html`（工具形态）的策划文档：定义"工具 vs 报告"的读者/任务/阅读路径差异，横向对比 Lighthouse、GTmetrix、三甲体检报告、麦肯锡金字塔、msprof-analyze advisor、Meta HTA、Nsight Systems、W&B Reports、Datadog 等竞品可借鉴点。 |
| Graph Evidence Workbench 构建契约 | `Profiling_Insight_and_Tool/AI_Profiling_Tool/js/graph-evidence/CONTRACT.md` | `graph-evidence-workbench.html` 多模块协作的共享接口契约（v0.1）：统一挂在 `window.GEW` 下的命名空间、状态结构、事件总线 `GEW.bus` 等约定，防止各模块各自发明全局变量/DOM ID/事件名。 |

## swimlane-skill-pack/ 文档

| 文档名 | 路径 | 主要内容 |
| --- | --- | --- |
| Profiler 视图层级结构大纲 | `Profiling_Insight_and_Tool/swimlane-skill-pack/structure.md` | 纯文本树形大纲，罗列 MindStudio Profiler 采集视图的层级结构（Process/Thread/PyTorch/CANN、Ascend Hardware/Stream、Communication、Overlap Analysis、HBM/LLC/QOS/AI Core/ACC PMU/Stars Soc/NPU MEM 等），作为泳道数据字段的参考草稿。 |
| Swimlane 标注 API 提案 | `Profiling_Insight_and_Tool/swimlane-skill-pack/ANNOTATION_API_PROPOSAL.md` | 为解决"读者难在泳道图中肉眼定位问题点"，给 `swimlane.js` 的 `SwimlaneRenderer` 新增 `setAnnotations()` 公开方法的变更提案：仅修改该文件、新增标注覆盖层渲染，向下兼容不影响现有渲染逻辑与数据格式。 |

## ParallelDemo/ 文档

| 文档名 | 路径 | 主要内容 |
| --- | --- | --- |
| Transformer 结构与并行策略知识库 | `Profiling_Insight_and_Tool/ParallelDemo/Transformer结构与并行策略知识库.md` | 面向初学者的模型结构 + 并行策略科普长文（v20260720，六部分）：以「我喜欢吃苹→果」贯穿全篇的最小案例，先讲 Transformer 结构（层/block/子层/算子/hidden size/MoE Router，含 openPangu-2.0-Flash 的 mHC 中间残差），再讲层↔权重矩阵↔算子的对应关系，然后是并行策略与 micro batch（DP/PP/EP、global_batch 与各并行维度的关系）、权重如何切到多卡（dp×pp×cp×tp 及通信规约出现的位置）、训练 vs 推理两种并行的差异，最后是跨硬件精度对齐的五个层次。 |

> 说明：范围外的第三方技能包文档（`AscendProfKit/`、`skills/craft-tool-launch-speech`、`skills/build-pptx`、`swimlane-skill-pack/skills/`）与 `Analysis Report/` 下的历次分析报告未列入，如需要可另行整理。

---

# 附录：常见问题 → 用哪个工具的哪张图解释

面向业务小白用户的「xxx 问题该拿什么图讲」速查表。原则：**结构性 / 配置性的「为什么会这样」用 Config & Relation Observer（含平面视图）的容量分段图讲；空间上「落在哪张卡」用 op-rank-time-wzhBranch 的 3D 映射讲；运行中「哪张卡出事、怎么出的事」用 Training Run Explainable Twin 的热力矩阵 + 定位链讲；真实数据核对交给 MindStudioNext v3 的 HBM 快照。** 后续遇到新问题按同一格式追加。

## 内存 / 显存类

| 问题 | 首选工具 · 图表 | 为什么是它 | 补充工具 |
| --- | --- | --- | --- |
| 某 rank 为什么 OOM | **Training Run Explainable Twin** → 「问题诊断」里的「显存 OOM」卡 + 聚光灯定位链（6 张举证图：显存曲线爬升、峰值超标 + 分配器碎片双因子判据等） | 唯一把 OOM 做成「表象 → 证据 → 根因」逐层讲故事的页面，左侧架构图同步高亮出事的那张卡，小白不用自己读数字 | 回到 **Config & Relation Observer** 的「单卡容量」等距投影图，看这套配置下该卡本来就贴着 HBM 上限还是有余量（越界判定一眼可见） |
| optimizer state 为什么异常占内存 | **Config & Relation Observer** → 「单卡容量」分段柱（权重 / 梯度 / 优化器状态 / 激活四段） | 页面直接给口径：Adam 优化器状态 = fp32 master weight + 两个 moment ≈ 12 B/参数，是权重段的好几倍；拨「关 / ZeRO-1 / FSDP2」三档，能看到优化器段随 ÷DP 缩小 —— 亲手体会「不切就是异常大」 | **MindStudioNext v3** 的 HBM 显存快照，看真实 run 里优化器张量的实际占比做对照 |
| TP/PP 为什么改变 activation/gradient footprint | **Config & Relation Observer · 平面视图** → 中间无限画布 + 「各 PP Stage 峰值」 | 平面视图把 TP/PP/DP/CP/EP 和配置项摆在同一张矩阵里：改 TP 看激活段按 TP 切分，改 PP 看每个 stage 只持有一段层且峰值随 1F1B 暖启动阶段不同而不同；叠加 `recompute` 四档看激活段再被重计算压下去 | **op-rank-time-wzhBranch** 的 1F1B chunk-level 调度泳道，解释「PP 为什么要囤好几个 micro-batch 的激活」这一时间维度的原因 |
| shard 到底落在哪个 rank | **op-rank-time-wzhBranch** → 3D 场景（正视 / 轴测）的模型层 × rank 映射 + 对象聚焦（Layers / Communication / Hardware） | 唯一把「某一层的某个算子 / 专家落在哪张卡」画成空间对应关系的页面，点 rank 就能看它持有哪些层、参与哪些 TP All-Reduce / EP All-to-All 组 | **Config & Relation Observer** 的一个 step 数据流播放，从配置角度看 DP×PP×TP×EP 的切分顺序（`world_size` 如何被分解） |
| checkpoint 为什么产生额外内存 | **Config & Relation Observer** → 「单卡容量」+ ZeRO-1 / FSDP2 三档切换 | 页面文案已解释：FSDP2 存 checkpoint 需 all-gather 回完整权重、ZeRO 分片状态保存时要临时聚合，都会在正常四段之外多出一段瞬时峰值；对照三档看「切得越细、保存时临时聚合越多」 | **Training Run Explainable Twin** 的「时光全景」浮窗，在时间轴上定位 checkpoint 事件与显存尖峰的重合 |
| 某个 rank 为什么和其他 rank memory 不一致 | **Training Run Explainable Twin** → 集群热力矩阵（跨卡显存占用） + MoE 专家负载（Router 倾斜）卡 | 热力矩阵先让小白一眼看出「哪张卡颜色不一样」；再点专家负载卡，多数不一致来自 EP 路由倾斜（热门专家所在 rank 收到更多 token）或 PP 首末 stage 多持 embedding / logits | **Config & Relation Observer** 的「各 PP Stage 峰值」看结构性不一致（stage 0 / 最后一个 stage 天然更重）；**MindStudioNext v3** HBM 快照做单卡精查 |
